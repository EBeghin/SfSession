{% extends 'base.html.twig' %}

{% block title %}Ajouter une session{% endblock %}

{% block body %}
    {# si edit (est vrai cad qu'il y a un id donc session existante) #}
    {% if edit %}
        <h1>Editer une session</h1>
    {% else %}
        <h1>Ajouter une session</h1>    
    {% endif %}

    {{ form_start(formAddSession) }}
        {# gestion des affichage par ligne qui regroupe des "colones" automatique selon le nombre d'éléments #}
        <div class="row">
            <div class="col-sm">
                {{ form_row(formAddSession.dateDebut) }}
            </div>
            <div class="col-sm">
                {{ form_row(formAddSession.dateFin) }}
            </div>
        </div>
        <div class="row">
            <div class="col-sm">
                {{ form_row(formAddSession.Denomination) }}
            </div>
            <div class="col-sm">
                {{ form_row(formAddSession.nombrePlaceMax) }}
            </div>
        </div>
        <div class="row">
            <div class="col-sm">
                {{ form_row(formAddSession.stagiaires) }}
            </div>
            <div class="col-sm">
                {{ form_row(formAddSession.formation) }}
            </div>
        </div>
       
        {# {% for stagiaire in formAddSession.stagiaires %} #}
            {# {{ dump(stagiaire) }} #}
            {# <select></select> #}
        {# {% endfor %} #}
        <div class="mt-3">
        {{ form_row(formAddSession.Valider) }}
        </div>

    {{ form_end(formAddSession) }}
{% endblock %}

