{% extends 'base.html.twig' %}

{% block title %}Liste des sessions{% endblock %}

{% block body %}
    <h1>Liste des sessions</h1>

    {% for session in sessions %}
        <a href="{{ path('showDetail_session', {'id': session.id}) }}"> {{ session.denomination }} </a><br><br>      
    {% endfor %}

{% endblock %}
